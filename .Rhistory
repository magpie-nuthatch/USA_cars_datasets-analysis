options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_postnormality.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$brand
year <- df$brand
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(x = brand,
g = price,
data = price
)
### LOADING LIBRARIES
library(tidyverse) # Loads tidyverse packages, mainly for ggplot2
library(epitools) # Loads epitools package for odds ratio calculation
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_postnormality.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$brand
year <- df$brand
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(x = brand,
g = price,
subset = price
)
### LOADING LIBRARIES
library(tidyverse) # Loads tidyverse packages, mainly for ggplot2
library(epitools) # Loads epitools package for odds ratio calculation
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_postnormality.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$brand
year <- df$brand
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(x = brand,
g = price,
subset = price
)
### LOADING LIBRARIES
library(tidyverse) # Loads tidyverse packages, mainly for ggplot2
library(epitools) # Loads epitools package for odds ratio calculation
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_postnormality.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$brand
year <- df$brand
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
data1 <- c(2.9, 3.0, 2.5, 2.6, 3.2) # group 1 data
data2 <- c(3.1, 3.3, 3.4, 2.8, 3.5) # group 2 data
data3 <- c(3.6, 3.8, 3.4, 3.7, 3.6) # group 3 data
# Combine all the data to a data frame
df <- data.frame(
values = c(data1, data2, data3),
group = factor(rep(c("Group1", "Group2", "Group3"), each = 5))
)
# Perform the Kruskal-Wallis test
result <- kruskal.test(values ~ group, data = df)
result
price <- df$price
brand <- df$brand
model <- df$brand
year <- df$brand
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
### LOADING LIBRARIES
library(tidyverse)
library(dunn.test)
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_postnormality.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$brand
year <- df$brand
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
### LOADING LIBRARIES
library(tidyverse)
library(dunn.test)
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_postnormality.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$brand
year <- df$brand
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(df$brand ~ df$price, data = df)
kruskal-wallis <- kruskal.test(df$brand ~ df$price, data = df)
kruskal-wallis <- kruskal.test(df$brand ~ df$price, data = df)
kruskal-wallis <- kruskall.test(df$brand ~ df$price, data = df)
kruskal-wallis <- kruskal.test(df$brand ~ df$price, data = df)
kruskal-wallis <- kruskal.test(df$brand ~ df$price, data = df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(brand ~ price, data = df)
kruskal-wallis <- kruskal.test(price ~ brand, data = df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal-wallis <- kruskal.test(price ~ brand, data = df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal_wallis <- kruskal.test(price ~ brand, data = df)
kruskal-wallis
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal_wallis <- kruskal.test(price ~ brand, data = df)
print(kruskal_wallis)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
kruskal_wallis <- kruskal.test(price ~ brand, data = df)
kruskal_wallis
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskall_wallis_dunn <- dunn.test(price ~ brand,
data - df,
list = TRUE)
kruskall_wallis_dunn <- dunn.test(price ~ brand,
data - df)
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
p.adjust.method = "none",
list = TRUE)
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
list = TRUE)
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
p.adjust.method = "none",
alternative = "greater",
list = TRUE)
### LOADING LIBRARIES
library(tidyverse)
library(dunn.test)
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_dunntest.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
# Kruskal-Wallis test and Dunn post-hoc test. Dunn post hoc tests requires a minimum of 3 values per category in x variable
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
list = TRUE)
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_dunntest.csv",
sep = ";")
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_dunntest.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
# Kruskal-Wallis test and Dunn post-hoc test. Dunn post hoc tests requires a minimum of 3 values per category in x variable
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
list = TRUE)
### LOADING LIBRARIES
library(tidyverse)
library(dunn.test)
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_dunntest.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
# Kruskal-Wallis test and Dunn post-hoc test. Dunn post hoc tests requires a minimum of 3 values per category in x variable
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
method = "bonferroni"
list = TRUE)
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
method = "bonferroni",
list = TRUE)
kruskal_wallis_dunn <- dunn.test(price ~ brand,
method = "bonferroni",
list = TRUE)
### LOADING LIBRARIES
library(tidyverse)
library(dunn.test)
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_dunntest.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
# Kruskal-Wallis test and Dunn post-hoc test. Dunn post hoc tests requires a minimum of 3 values per category in x variable
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskal_wallis_dunn <- dunn.test(price ~ brand,
data = df
method = "bonferroni",
dunn.test(price ~ brand, data = df, method = "bonferroni")
dunn.test(price ~ brand, method = "bonferroni")
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
method = "bonferroni"
list = TRUE)
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
method = "bonferroni"
list = TRUE)
### LOADING LIBRARIES
library(tidyverse)
library(dunn.test)
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_postnormality.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
# Kruskal-Wallis test and Dunn post-hoc test. Dunn post hoc tests requires a minimum of 3 values per category in x variable
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
list = TRUE)
kruskal_wallis_dunn <- dunn.test(kruskal_wallis$statistic,
g = kruskal_wallis$group,
list = TRUE)
### LOADING LIBRARIES
library(tidyverse)
library(dunn.test)
### SETTING OPTIONS
options(
scipen = 999 # Turns off scientific notation
)
### SETTING WORKING DIRECTORY AND LOADING FILES
setwd(
"~/T99/USA_cars_datasets-analysis")
df <- read.csv(
"csv_trans\\USA_cars_datasets_trans_dunntest.csv",
sep = ";")
### GATHERING DESCRIPTIVES OF DATAFRAME
head(df,
n = 6)
summary(df)
nrow(df)
ncol(df)
price <- df$price
brand <- df$brand
model <- df$model
year <- df$year
mileage <- df$mileage
state <- df$state
color_simple <- df$color_simple
condition_simple <- df$condition_simple
# Kruskal-Wallis test and Dunn post-hoc test. Dunn post hoc tests requires a minimum of 3 values per category in x variable
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskal_wallis_dunn <- dunn.test(brand,
g = price,
list = TRUE)
kruskal_wallis <- kruskal.test(price ~ brand,
data = df)
kruskal_wallis
kruskal_wallis_dunn <- dunn.test(price,
g = brand,
list = TRUE)
kruskall_wallis_dunn
kruskal_wallis_dunn <- dunn.test(price,
g = brand,
list = TRUE)
kruskal_wallis_dunn
kruskal_wallis_dunn <- dunn.test(price,
g = brand,
method = "bonferroni",
list = TRUE)
kruskal_wallis_dunn
